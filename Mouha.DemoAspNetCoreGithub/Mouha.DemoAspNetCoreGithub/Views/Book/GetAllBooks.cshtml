@{
    ViewData["Title"] = "Tous les livres";
}

@model IEnumerable<BookModel>

@*@{
        var parameters = new Dictionary<string, string>()
        {
            {"nom", "Mouhamed" },
            {"Compagnie", "WebMouha" },
            {"appNom", "GestionLivre" }
        };

        asp-all-route-data="@parameters"
        asp-fragment="LivreSimilaires"
}*@

<div class="container">
    <h3 class="display-4">Tous les livres</h3>
    <div class="row">

        @foreach (var livre in Model)
        {
            <div class="col-md-4">
                <div class="card mb-4 shadow-sm">
                    <svg class="bd-placeholder-img card-img-top" width="100%" height="225" xmlns="http://www.w3.org/2000/svg" preserveAspectRatio="xMidYMid slice" focusable="false" role="img" aria-label="Placeholder: Thumbnail"><title>Placeholder</title><rect width="100%" height="100%" fill="#55595c"></rect><text x="50%" y="50%" fill="#eceeef" dy=".3em">Thumbnail</text></svg>
                    <div class="card-body">
                        <h3 class="card-title">@livre.Title</h3>
                        <p class="card-text">@livre.Description</p>
                        <div class="d-flex justify-content-between align-items-center">
                            <div class="btn-group">
                                @*<a asp-controller="book" asp-action="getbook" asp-route-id="@livre.Id"
                                   asp-route-nomDuLivre="@livre.Title"
                                   class="btn btn-sm btn-outline-secondary">Details</a>*@

                                <a asp-route="livreDetailRoute" asp-route-id="@livre.Id"
                                   class="btn btn-sm btn-outline-secondary">Details</a>

                                
                            </div>
                            <small class="text-muted">@livre.Author</small>
                        </div>
                    </div>
                </div>
                @*<partial name="_bookThumbnail" model="new BookModel()" />*@
            </div>
        }


    </div>
</div>